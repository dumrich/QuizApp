export declare type UpdateStatus = 'PENDING' | 'ERROR' | 'DONE' | 'UPTODATE';
export interface UpdateStatusResult {
    error?: string;
    status: UpdateStatus;
}
export interface UpdateManifest {
    version: string;
    date: string;
    body: string;
}
export interface UpdateResult {
    manifest?: UpdateManifest;
    shouldUpdate: boolean;
}
/**
 * Install the update if there's one available.
 *
 * @return A promise indicating the success or failure of the operation.
 */
export declare function installUpdate(): Promise<void>;
/**
 * Checks if an update is available.
 *
 * @return Promise resolving to the update status.
 */
export declare function checkUpdate(): Promise<UpdateResult>;
